[gd_scene load_steps=6 format=3 uid="uid://c7su215b2bkb7"]

[ext_resource type="Script" uid="uid://dlnvpi6oglckv" path="res://NPCs/bashed_npc.gd" id="1_awpie"]
[ext_resource type="ArrayMesh" uid="uid://dftp60qr51rp2" path="res://Models/MC/GENERIC_RUN.vox" id="1_y3m4d"]

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_awpie"]
radius = 1.01202
height = 3.21778

[sub_resource type="Shader" id="Shader_bkfkh"]
code = "// NOTE: Shader automatically converted from Godot Engine 4.4.1.stable's StandardMaterial3D.

shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_back, diffuse_burley, specular_schlick_ggx;

uniform vec4 albedo : source_color;
uniform sampler2D texture_albedo : source_color, filter_linear_mipmap, repeat_enable;
uniform ivec2 albedo_texture_size;
uniform float point_size : hint_range(0.1, 128.0, 0.1);

uniform float roughness : hint_range(0.0, 1.0);
uniform sampler2D texture_metallic : hint_default_white, filter_linear_mipmap, repeat_enable;
uniform vec4 metallic_texture_channel;
uniform sampler2D texture_roughness : hint_roughness_r, filter_linear_mipmap, repeat_enable;

uniform float specular : hint_range(0.0, 1.0, 0.01);
uniform float metallic : hint_range(0.0, 1.0, 0.01);

uniform vec3 uv1_scale;
uniform vec3 uv1_offset;
uniform vec3 uv2_scale;
uniform vec3 uv2_offset;

instance uniform vec4 skin;
instance uniform vec4 hair;
instance uniform vec4 hat;
instance uniform vec4 shirt;
instance uniform vec4 pants;
instance uniform vec4 shoes;

void vertex() {
	if (COLOR.r+COLOR.b+COLOR.g < 0.1 && COLOR.r+COLOR.b+COLOR.g > 0.0) {
		COLOR = hair;
	} else if (COLOR == vec4(1.0, 0.0, 0.0, 1.0)) {
		COLOR = skin;
	} else if (COLOR == vec4(1.0, 1.0, 0.0, 1.0)) {
		COLOR = hat;
	} else if (COLOR == vec4(0.0, 1.0, 0.0, 1.0)) {
		COLOR = shirt;
	} else if (COLOR == vec4(0.0, 0.0, 1.0, 1.0)) {
		COLOR = pants;
	} else if (COLOR == vec4(0.0, 1.0, 1.0, 1.0)) {
		COLOR = shoes;
	}
	// Vertex Color is sRGB: Enabled
	if (!OUTPUT_IS_SRGB) {
		COLOR.rgb = mix(
				pow((COLOR.rgb + vec3(0.055)) * (1.0 / (1.0 + 0.055)), vec3(2.4)),
				COLOR.rgb * (1.0 / 12.92),
				lessThan(COLOR.rgb, vec3(0.04045)));
	}

	UV = UV * uv1_scale.xy + uv1_offset.xy;
}

void fragment() {
	vec2 base_uv = UV;

	vec4 albedo_tex = texture(texture_albedo, base_uv);

	// Vertex Color Use as Albedo: Enabled
	albedo_tex *= COLOR;

	ALBEDO = albedo.rgb * albedo_tex.rgb;

	float metallic_tex = dot(texture(texture_metallic, base_uv), metallic_texture_channel);
	METALLIC = metallic_tex * metallic;
	SPECULAR = specular;

	vec4 roughness_texture_channel = vec4(1.0, 0.0, 0.0, 0.0);
	float roughness_tex = dot(texture(texture_roughness, base_uv), roughness_texture_channel);
	ROUGHNESS = roughness_tex * roughness;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_nt77d"]
render_priority = 0
shader = SubResource("Shader_bkfkh")
shader_parameter/albedo = Color(1, 1, 1, 1)
shader_parameter/albedo_texture_size = Vector2i(0, 0)
shader_parameter/point_size = 1.0
shader_parameter/roughness = 1.0
shader_parameter/metallic_texture_channel = Vector4(1, 0, 0, 0)
shader_parameter/specular = 0.5
shader_parameter/metallic = 0.0
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/uv2_scale = Vector3(1, 1, 1)
shader_parameter/uv2_offset = Vector3(0, 0, 0)

[node name="BashedNPC" type="RigidBody3D"]
mass = 0.004
script = ExtResource("1_awpie")

[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.5, 0)
shape = SubResource("CapsuleShape3D_awpie")

[node name="Meshes" type="Node3D" parent="."]

[node name="McRun" type="MeshInstance3D" parent="Meshes"]
mesh = ExtResource("1_y3m4d")
skeleton = NodePath("../..")
surface_material_override/0 = SubResource("ShaderMaterial_nt77d")
